<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-2xl font-bold text-cyan-300">Analysis Reports</h2><a href="{{ route('reports.create') }}" class="rounded bg-cyan-500 px-4 py-2 text-slate-950">Run Analysis</a></div></x-slot>
<div class="space-y-3">@foreach($reports as $report)<a href="{{ route('reports.show',$report) }}" class="block rounded border border-slate-800 bg-slate-900 p-4">{{ $report->clubProfile->club_name }} vs {{ $report->player->name }} <span class="text-emerald-300">{{ $report->final_fit_score }}</span></a>@endforeach</div>{{ $reports->links() }}</x-app-layout>
