<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-2xl font-bold text-cyan-300">Players</h2><a href="{{ route('players.create') }}" class="rounded bg-cyan-500 px-4 py-2 text-slate-950">Add Player</a></div></x-slot>
<div class="space-y-3">@foreach($players as $player)<div class="rounded border border-slate-800 bg-slate-900 p-4 flex justify-between"><div><p class="font-semibold">{{ $player->name }} ({{ $player->position }})</p><p class="text-slate-400">Age {{ $player->age }} · Value {{ $player->market_value }}</p></div><div class="space-x-2"><a href="{{ route('players.show',$player) }}">View</a><a href="{{ route('players.edit',$player) }}">Edit</a></div></div>@endforeach</div>{{ $players->links() }}</x-app-layout>
